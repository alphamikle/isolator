name: isolator
description: Isolator is a package, which offer to you a simple way for creating two-component states with isolated part and frontend part of any kind - BLoC, MobX, ChangeNotifier and others
version: 2.0.8

repository: https://github.com/alphamikle/isolator
homepage: https://alphamikle.github.io/high_low/#/

environment:
  sdk: ">=2.19.5 <4.0.0"

dependencies:
  analyzer: ^6.4.1
  build: ^2.4.1
  flutter:
    sdk: flutter
  meta: ^1.9.0
  source_gen: ^1.5.0

dev_dependencies:
  build_runner: ^2.4.9
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
