library isolator;

import 'package:isolator/src/isolator/isolator_abstract.dart';

/// Abstract method to create isolator (web and native)
Isolator createIsolator() => throw UnimplementedError(
      'This is Isolator creation stub',
    );
